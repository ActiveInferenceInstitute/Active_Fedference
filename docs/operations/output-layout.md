# Output layout

Everything under the project-local `output/` and the template copy at
`output/working/active_fedference/` is generated by the pipeline. The project
commits refreshed output snapshots for reviewers, but the files remain
producer-owned artifacts: never hand-edit generated JSON/PNG/PDF/HTML to pass a
gate.

Local page rasters, extracted review text, and temporary test locks belong under
`.tmp/`. That directory is ignored and is never a release input.

Installed `fedference run` and `fedference benchmark` commands do not write into
this tree. They require a caller-owned empty `--output-dir`, reject any target
under committed `output/`, and place `report.json` plus `receipt.json` there.
They also write `config.json`; the receipt binds all three files and its
canonical configuration hash.
Only a separately reviewed producer integration may promote a confirmatory pack
into the reviewer snapshot.

## Working directory (`output/`)

| Path | Producer | Consumer |
| --- | --- | --- |
| `reports/belief_sharing.json` | `analysis/workflow._belief_sharing_report` | Figures, `manuscript_variables` |
| `reports/language_acquisition.json` | `_language_report` | Figures, tokens |
| `reports/emergence.json` | `_emergence_report` | Figures, tokens |
| `reports/robustness_sweep.json` | `_robustness_report` | Figures, tokens |
| `reports/efe_decomposition.json` | `_efe_terms` | EFE figure |
| `reports/robust_influence_weights.json` | `_influence_weights` | Influence figure |
| `reports/bnn_robustness.json` | `_bnn_report` | Logistic-regression robustness figure and tokens |
| `reports/variational_aggregation.json` | `_variational_aggregation_report` | Descent/redescending-weight figures, `VARIATIONAL_*` tokens |
| `reports/contamination_gallery.json` | `_contamination_gallery_report` | Gallery figure, `GALLERY_*` tokens |
| `reports/robustness_onset.json` | `_robustness_onset_report` | Onset figure, `ONSET_*` tokens |
| `reports/moving_world.json` | `_moving_world_report` | Moving-world figure, `MOVING_*` tokens (V4) |
| `reports/parameter_recovery.json` | `_parameter_recovery_report` | Parameter-recovery figure, `PARAM_RECOVERY_*` tokens |
| `reports/heuristic_characterization.json` | `heuristic_characterization_report` | Heuristic influence, finite-breakdown, attack-grid, and no-claim diagnostics |
| `reports/hierarchical_bmr.json` | `hierarchical_bmr_report` | Hierarchical BMR figure and supplement |
| `reports/complexity_scaling.json` | `run_complexity_scaling` | Source-bound asymptotic orders, timing-grid configuration, portable machine metadata (including the interpreter basename, never its contributor-local absolute path), and repeated scaling measurements |
| `reports/bnn_torch.json` | `_bnn_torch_report` | PyTorch MLP FedGVI complement, `BNN_TORCH_*` tokens |
| `reports/hierarchical_world.json` | `hierarchical_world_report` | Hierarchical POMDP figure, `HIER_*` tokens |
| `reports/nlevel3_world.json` | `nlevel3_world_report` | 3-level POMDP figure, `NLEVEL3_*` tokens |
| `reports/cross_study_summary.json` | `summarize_cross_study` | Cross-study summary figure, `CROSS_STUDY_*` tokens |
| `reports/disjoint_fov_world.json` | `disjoint_fov_report` | Disjoint-FOV geometry figure, `V4_*` tokens |
| `figures/free_energy_comparison.png` | `src/figures/free_energy_comparison.py` | Manuscript |
| `figures/system_overview.png` / `figures/system_overview.pdf` | `src/figures/system_overview.py` | Introductory system overview |
| `figures/graphical_abstract.png` / `figures/graphical_abstract.pdf` | `src/figures/graphical_abstract.py` | Graphical abstract and cover source |
| `figures/belief_heatmap.png` | `src/figures/belief_heatmap.py` | Manuscript |
| `figures/language_kl_decay.png` | `src/figures/language_kl_decay.py` | Manuscript |
| `figures/emergence_bmr.png` | `src/figures/emergence_bmr.py` | Manuscript |
| `figures/robustness_sweep.png` | `src/figures/robustness_sweep.py` | Manuscript |
| `figures/robust_influence_weights.png` | `src/figures/robust_influence_weights.py` | Manuscript |
| `figures/bnn_robustness.png` | `src/figures/bnn_robustness.py` | Manuscript |
| `figures/efe_decomposition.png` | `src/figures/efe_decomposition.py` | Manuscript |
| `figures/aggregation_descent.png` | `src/figures/aggregation_descent.py` | Manuscript |
| `figures/bounded_influence.png` | `src/figures/bounded_influence.py` | Manuscript |
| `figures/contamination_gallery.png` | `src/figures/contamination_gallery.py` | Manuscript |
| `figures/descent_comparison.png` | `src/figures/descent_comparison.py` | Manuscript |
| `figures/robustness_onset.png` | `src/figures/robustness_onset.py` | Manuscript |
| `figures/disjoint_fov_world.png` | `src/figures/disjoint_fov_world.py` | Moving-world geometry support |
| `figures/moving_world.png` / `.pdf` | `src/figures/moving_world.py` | Manuscript PNG plus archival PDF (V4) |
| `figures/hierarchical_pomdp.png` | `src/figures/hierarchical_pomdp.py` | 2×3 six-panel hierarchical belief dynamics: top row = 2-level (Study 6), bottom row = 3-level (Study 7) (V2) |
| `figures/hierarchical_bmr.png` | `src/figures/hierarchical_bmr.py` | Hierarchical BMR diagnostics |
| `figures/heuristic_breakdown.png` | `src/figures/heuristic_breakdown.py` | Heuristic influence, finite-breakdown, and finite-search attack-grid diagnostics |
| `figures/sensitivity_heatmap.png` | `src/figures/sensitivity_heatmap.py` | Study 8 sensitivity |
| `figures/cross_study_summary.png` | `src/figures/cross_study_summary.py` | Cross-study summary |
| `figures/parameter_recovery.png` | `src/figures/parameter_recovery.py` | Study 9 parameter recovery |
| `figures/complexity_scaling.png` / `.pdf` | `src/figures/complexity_scaling.py` | Complexity orders and machine-scaling diagnostic |
| `data/manuscript_variables.json` | `z_generate_manuscript_variables.py` | Debugging token map |
| `data/pipeline_provenance.json` | `02_run_analysis.py`, `z_generate_manuscript_variables.py`, `record_pipeline_stage.py` | Schema-2 content-hashed, byte-idempotent stage freshness validation |
| `manuscript/*.md` | Token substitution | PDF render (stage 5) |
| `pdf/*` | `scripts/pipeline/stage_03_render.py` | Validation, copy stage |
| `slides/*` | Template render stage (per-section Beamer decks) | Release bundle |
| `release/manifest.json`, `release/sha256sums.txt`, `release/README.md` | `scripts/build_release.py` | `scripts/build_release.py --verify`, CI round-trip |
| `docs/api_reference.md` | `generate_api_docs.py` | Human/API reference (optional) |
| `web/*`, `web/figures/*`, `tex/*`, `llm/*`, `logs/*`, `reports/validation_*` | Pipeline infra and web package prep | Validation reports and browser package |

All `reports/*.json` payloads are validated against typed schemas at the
single write boundary (`src/analysis/report_schemas.py`): a study whose payload
drifts from its declared shape fails at write time, and each figure generator's
consumed fields are checked against an explicit dependency contract.

Full producer graph: [`../core/experiments-and-artifacts.md`](../core/experiments-and-artifacts.md).

## Template copy (`output/working/active_fedference/`)

Copied by `scripts/pipeline/stage_05_copy.py` from the template repository
root:

```
output/working/active_fedference/
├── pdf/active_fedference_combined.pdf
├── figures/
├── data/
└── reports/
```

## Regeneration rules

1. **Never hand-edit** generated JSON/PNG/PDF to pass a gate — fix the producer
   in `src/fedference/` or `src/analysis/workflow.py`. Hand edits are also
   caught mechanically: the release manifest records per-artifact SHA-256
   digests plus a source/manuscript/documentation/producer provenance
   fingerprint, pipeline profile, generator version, and individual input
   digests, so
   `scripts/build_release.py --verify` first requires fresh publication-profile
   analysis, test/coverage, hydration, and render receipts, then fails on any
   altered artifact or on a
   bundle built from a different evidence or producer state; changed input
   paths are included in the failure diagnostic. This is a local reviewer-bundle
   gate, not external publication authorization.
2. After changing `manuscript/config.yaml` experiment block, re-run analysis
   **before** variable hydration and PDF render.
3. Adding a new artifact requires updating `manuscript_variables.py` (if cited
   in prose), the write-boundary schema in `src/analysis/report_schemas.py`
   (if it is a JSON report), and
   [`../core/experiments-and-artifacts.md`](../core/experiments-and-artifacts.md).
4. After the final template copy, web output must be prepared with
   `scripts/prepare_web_package.py` before browser inspection or render-receipt
   recording so local figures and normalized crossrefs are present.
   `scripts/validate_web_package.py` then enforces the semantic HTML subset defined in
   [`../manuscript/accessibility.md`](../manuscript/accessibility.md).
5. After regenerating outputs, rebuild the release bundle
   (`uv run python scripts/build_release.py`) so the manifest and fingerprint
   track the new state. The default unreleased bundle deliberately records no
   wall-clock timestamp, and a second no-op build must be byte-identical. Use
   `--timestamp` or `SOURCE_DATE_EPOCH` only after release approval.
6. Record the external template render boundary only after the final stages
   03–05, web preparation, and rendered-surface validation finish; then run
   `uv run python scripts/validate_pipeline_freshness.py` before the release
   build. The receipt hashes `output/web/` as well as the PDF/slide surfaces,
   so an earlier receipt would be stale as soon as web preparation rewrites it.
   The receipt is content-bound and must not be hand-edited.

## Regeneration commands

```bash
# Analysis only
uv run python scripts/02_run_analysis.py

# Source-current variables + renderer passes
# Follow the explicit provisional-render -> coverage receipt -> final-render
# sequence in ../manuscript/rendering_pipeline.md. Both calls to stage 03 use
# --skip-manuscript-hydration because that guide performs hydration explicitly.

# Full core pipeline
TEMPLATE_REPO=/path/to/template
cd "$TEMPLATE_REPO"
uv run python scripts/runner/execute_pipeline.py --project working/active_fedference --core-only
```

The combined core command is for exploratory template work. It is not a
substitute for the source-current two-pass sequence because it does not make
the provisional and receipt-backed hydration boundary explicit.

## See also

- [`troubleshooting.md`](troubleshooting.md)
- [`../manuscript/rendering_pipeline.md`](../manuscript/rendering_pipeline.md)
- [`../manuscript/accessibility.md`](../manuscript/accessibility.md)
